import styled from 'styled-components';

const Wrap = styled.div`
  text-align: center;
  font-style: italic;
  font-size: ${(props) => props.theme.font.sizeMd};
  line-height: 1;
  padding: 8px 14px;
`;

export { Wrap };
